# GENERATED FILE — do not edit by hand.
# Source: packages/factgraph-api/src/openapi.ts (buildOpenApiDocument)
# Regenerate: npm run gen:openapi --workspace=rules-visualizer-factgraph-api
openapi: 3.1.0
info:
  title: Factgraph API
  version: 0.1.0
  description: |-
    A generic HTTP adapter for Fact Graph rulesets defined in this repository.

    One endpoint shape (`POST /v1/factgraph/{rulesetId}/query`) handles every kind of query: top-level outputs, intermediate gates, per-member facts, partial input, full determinations. Multiple targets per call.

    When inputs are missing, the response includes a precise list of what is still needed — letting partner UIs drive intake forms dynamically from the rules rather than maintaining a parallel schema.

    See the [partner-facing docs](https://github.com/Gary-Community-Ventures/rules-visualizer/tree/main/packages/factgraph-api) for concepts, worked SNAP examples, and the changelog.
  license:
    name: MPL-2.0
servers:
  - url: https://rules-visualizer-factgraph-api-f0c14673cf3a.herokuapp.com
    description: Production deploy
  - url: http://localhost:5002
    description: Local dev server
tags:
  - name: Meta
    description: Liveness probes and health checks.
  - name: Discovery
    description: Find available rulesets and inspect their schema.
  - name: Query
    description: Evaluate fact-graph targets against caller-provided input.
components:
  schemas:
    TraceCitation:
      type: object
      description: A policy citation resolved from the ruleset's references.json.
        Surfaced inline on trace nodes whose underlying fact has policy
        mappings.
      required:
        - sectionId
        - documentTitle
      properties:
        sectionId:
          type: string
        documentTitle:
          type: string
        documentUrl:
          type: string
        comment:
          type: string
        page:
          type: integer
    TraceNode: {}
    DecidingPathStep:
      type: object
      description: One step in the deciding chain for a queried target. Path-bearing
        nodes only — anonymous sub-expressions are skipped in favor of the
        surrounding fact they live under.
      required:
        - path
        - value
        - op
      properties:
        path:
          type: string
        name:
          type: string
        value: {}
        op:
          type: string
          description: Operator at this point in the chain — useful for picking icons or
            color in a UI.
    DecidingPath: {}
    QueryRequest:
      type: object
      properties:
        targets:
          type: array
          items:
            type: string
            minLength: 1
          minItems: 1
        inputs:
          type: object
          additionalProperties:
            anyOf:
              - type: array
                items:
                  type: object
                  properties:
                    id:
                      type: string
                      minLength: 1
                  additionalProperties: {}
              - {}
              - type: "null"
        include:
          type: array
          items:
            type: string
        metadata: {}
      required:
        - targets
      additionalProperties: false
      description: Evaluate one or more fact-graph nodes against partial input.
    ProblemDetails:
      type: object
      properties:
        type:
          type: string
          format: uri
          description: A URI reference identifying the problem type.
          example: https://tools.ietf.org/html/rfc9457
        title:
          type: string
          description: Short, human-readable summary of the problem type.
          example: Invalid request body
        status:
          type: integer
          description: HTTP status code generated by the origin server.
          example: 400
        detail:
          type: string
          description: Human-readable explanation specific to this occurrence.
        errors:
          type: array
          items:
            type: object
            properties:
              path:
                type: string
                description: JSON path to the field that failed validation. Empty string for
                  root-level errors.
              message:
                type: string
            required:
              - path
              - message
          description: Present on 400 responses from request validation. Each entry
            pinpoints a specific failing field.
      required:
        - type
        - title
        - status
        - detail
      description: RFC 9457 Problem Details error response.
    PerMemberValue:
      type: array
      items:
        type: object
        properties:
          memberId:
            type: string
            description: Caller-provided id from the entity row, or auto-generated
              `member-N` positional id when the caller didn't supply one.
          value:
            description: The fact value for this member.
        required:
          - memberId
      description: Per-member fact value. Returned for any target path that is
        collection-scoped (contains the collection wildcard segment). Each entry
        corresponds to one row from the request's `inputs[<collection-root>]`
        array.
    MissingInput:
      type: object
      properties:
        path:
          type: string
          description: The fact-graph path the caller would need to supply.
        name:
          type: string
          description: Human-readable display name (from the rules XML).
        description:
          type: string
          description: Free-form description of what this input represents.
        dataType:
          type: string
          description: Writable type name from the fact graph (e.g. "Dollar", "Boolean",
            "Int", "Enum").
          example: Dollar
        enumOptions:
          type: array
          items:
            type: string
          description: When dataType is "Enum" and the allowed values are statically
            resolvable, the list of accepted strings.
      required:
        - path
        - name
        - dataType
      description: One writable input the caller would need to supply to make a
        requested target resolvable. Deduped across all unresolved targets in
        the request.
    SupportingFact:
      type: object
      properties:
        path:
          type: string
        name:
          type: string
        value:
          description: Scalar value, or a PerMemberValue array for collection-scoped facts.
      required:
        - path
        - name
      description: One fact from the dependency tree of a queried target that resolved
        with the provided inputs. Only included when `include` contained
        `"supportingFacts"`.
    QueryResponse:
      type: object
      properties:
        status:
          type: string
          enum:
            - complete
            - incomplete
          description: "`complete` iff every requested target resolved. Otherwise
            `incomplete`."
        rulesetVersion:
          type: string
          description: The ruleset id that was evaluated. Useful for clients that pin to a
            specific version (e.g. snap-fy2026 vs snap-fy2027).
          example: snap-fy2026
        values:
          type: object
          additionalProperties: {}
          description: One entry per requested target. Value is `null` when the engine
            couldn't resolve that target with the provided inputs. Scalar facts
            return primitives; collection-scoped facts return PerMemberValue
            arrays.
        metadata:
          description: Echoed unchanged from the request. Omitted if the request didn't
            send one.
        missingInputs:
          type: array
          items:
            $ref: "#/components/schemas/MissingInput"
          description: Present iff status === "incomplete". Union of writables still
            needed across all unresolved targets, deduped by path.
        supportingFacts:
          type: array
          items:
            $ref: "#/components/schemas/SupportingFact"
          description: Present iff request.include contained "supportingFacts". Every fact
            in the targets' dependency trees that resolved.
        traces:
          type: object
          additionalProperties:
            $ref: "#/components/schemas/TraceNode"
          description: Present iff request.include contained "trace". Keyed by target
            path; each entry is a recursive TraceNode tree explaining how that
            target's value was derived.
        decidingPaths:
          type: object
          additionalProperties:
            $ref: "#/components/schemas/DecidingPath"
          description: Present iff request.include contained "trace". For each requested
            target, a compact ordered chain of path-bearing nodes from the
            target down to the deepest single-leaf cause. Powers one-line
            headline rendering — no need to walk the full TraceNode tree for the
            simple "this is why" sentence.
      required:
        - status
        - rulesetVersion
        - values
      description: Result of evaluating one or more fact-graph targets. Same shape
        regardless of complete vs incomplete; clients branch on status and on
        null values per target.
    RulesetSummary:
      type: object
      properties:
        id:
          type: string
        name:
          type: string
        format:
          type: string
          enum:
            - factGraph
      required:
        - id
        - name
        - format
  securitySchemes:
    bearerAuth:
      type: http
      scheme: bearer
      description: Required on every /v1/* request when the deployed API has
        `API_BEARER_TOKEN` configured. Local dev runs without auth by default.
  parameters: {}
paths:
  /health:
    get:
      summary: Liveness probe.
      description: Unauthenticated. Returns `{"status":"ok"}` when the server is up.
        Used by deployment tooling.
      tags:
        - Meta
      responses:
        "200":
          description: Server is up.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: string
                    enum:
                      - ok
                required:
                  - status
  /v1/factgraph/rulesets:
    get:
      summary: List every loaded ruleset.
      description: Use the `id` field of each entry as the `:rulesetId` URL parameter
        in subsequent calls.
      tags:
        - Discovery
      security:
        - bearerAuth: []
      responses:
        "200":
          description: List of rulesets.
          content:
            application/json:
              schema:
                type: object
                properties:
                  rulesets:
                    type: array
                    items:
                      $ref: "#/components/schemas/RulesetSummary"
                required:
                  - rulesets
        "401":
          description: Authentication required.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
  /v1/factgraph/{rulesetId}/schema:
    get:
      summary: Full node index for a ruleset.
      description: Returns every fact in the ruleset with its path, name, description,
        dependencies, data type, and (when mapped) policy citations. For day-one
        discovery prefer POST /query with no inputs — it returns only what is
        needed for a specific target.
      tags:
        - Discovery
      security:
        - bearerAuth: []
      parameters:
        - schema:
            type: string
            example: snap-fy2026
          required: true
          name: rulesetId
          in: path
      responses:
        "200":
          description: Ruleset model.
          content:
            application/json:
              schema:
                type: object
                properties:
                  id:
                    type: string
                  name:
                    type: string
                  format:
                    type: string
                    enum:
                      - factGraph
                  nodes:
                    type: object
                    additionalProperties: {}
                required:
                  - id
                  - name
                  - format
                  - nodes
                description: Shape mirrors `rules-visualizer-shared-types`'s Model type. Each
                  node carries its content, dependencies, and (optionally)
                  references.
        "401":
          description: Authentication required.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
        "404":
          description: Ruleset not found.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
  /v1/factgraph/{rulesetId}/query:
    post:
      summary: Evaluate one or more fact-graph targets.
      description: >-
        Pass any subset of inputs (scalars and collection rows live in the same
        `inputs` map, keyed by fact path or collection root). The engine
        resolves what it can; the response carries values for resolved targets,
        and (when anything is missing) a deduped list of writables the caller
        would still need to supply.


        Targets can be top-level outputs (`/eligible`, `/snap`), intermediate
        gates (`/grossIncomeLimit`, `/meetsAssetTest`), or per-member facts.
        Per-member targets return arrays of `{memberId, value}` correlated to
        caller-provided `id` fields on entity rows.


        See the `examples-snap.md` doc in this repo for worked walkthroughs.
      tags:
        - Query
      security:
        - bearerAuth: []
      parameters:
        - schema:
            type: string
            example: snap-fy2026
          required: true
          name: rulesetId
          in: path
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/QueryRequest"
      responses:
        "200":
          description: Evaluation result. Always 200; `status` field discriminates
            complete vs incomplete.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/QueryResponse"
        "400":
          description: Request body failed validation. The response includes both a
            human-readable `detail` string and a machine-readable `errors[]`
            array keyed by field path.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
        "401":
          description: Authentication required.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
        "404":
          description: Ruleset or target not found. `detail` lists every bad path so the
            client can fix typos in one shot.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
webhooks: {}
